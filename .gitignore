# Arquivo gerado (bundle único com dados reais embutidos) — não versionar
ABRIR NO CELULAR.html

# Backups reais exportados (dados de obras/clientes) - não devem ir para um repositório público
dados/
